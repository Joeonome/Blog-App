<%- include("partials/header"); %>

<h1>Home</h1>
<p><%= someContent %></p>

<% posts.forEach((post) => { %>
   <h1><%= post.title %></h1> 
     <p>
        <%= post.body.substring(0, 100) + '...' %>
        <a href="/posts/<%= post.title %>">Read More</a>
    </p> 
<% }) %>
<br>

<a style="text-decoration: none" href="/compose"><button class='btn btn-primary'>Compose</button></a>

<% console.log(posts); %>

<%- include("partials/footer"); %>